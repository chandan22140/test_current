program: train_glue_deberta.py
method: bayes
project: glue-deberta-mrpc-butterfly-sequential
metric:
  name: best_metric
  goal: maximize

parameters:
  task:
    value: mrpc
  
  method:
    value: way1
  
  rank:
    value: 768

  lr:
    distribution: log_uniform_values
    min: 0.00035
    max: 0.0035
  
  epochs:
    value: 20
  
  weight_decay:
    min: 1e-05
    max: 0.001
    distribution: log_uniform_values

  batch-size:
    value: 32

command:
  - ${env}
  - python
  - ${program}
  - ${args}
  - --use-butterfly
  - --butterfly-sequential
