program: train_glue_deberta.py
method: bayes
project: glue-deberta-mrpc-way1-r128
metric:
  name: best_metric
  goal: maximize

parameters:
  task:
    value: mrpc
  
  method:
    value: way1
  
  rank:
    value: 128

  lr:
    distribution: log_uniform_values
    min: 0.001
    max: 0.1
  
  epochs:
    value: 20
  
  weight_decay:
    min: 0.0001
    max: 0.01
    distribution: log_uniform_values

  batch-size:
    value: 32

command:
  - ${env}
  - python
  - ${program}
  - ${args}
