program: train_glue_deberta.py
method: bayes
project: glue-deberta-stsb
metric:
  name: best_metric
  goal: maximize

parameters:
  task:
    value: stsb
  
  method:
    values: ["way0"]
  
  batch-size:
    values: [32]
  
  lr:
    distribution: log_uniform_values
    min: 1e-4
    max: 1e-3
  
  rank:
    values: [85]
  
  epochs:
    value: 25
  
  orthogonality_weight:
    min: 0.001
    max: 0.05
    distribution: uniform

  weight_decay:
    min: 0.00001
    max: 0.001
    distribution: log_uniform_values

command:
  - ${env}
  - python
  - ${program}
  - ${args}
