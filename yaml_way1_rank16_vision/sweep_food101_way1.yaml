program: train_vit_rotational.py
method: bayes
project: PiSSA-food101-way1
metric:
  name: best_accuracy
  goal: maximize

parameters:
  model:
    value: google/vit-base-patch16-224
  
  dataset:
    value: "food101"
  
  learning-rate:
    min: 0.0001
    max: 0.05
    distribution: log_uniform_values
  
  weight-decay:
    min: 0.00001
    max: 0.05
    distribution: log_uniform_values
  
  lr-ratio-s:
    value: 10.0
    

  total-cycles:
    values: [3]

  rank:
    values: [16]
  
  method:
    value: way1
  
  epochs:
    value: 10
  
  batch-size:
    values: [32]

command:
  - ${env}
  - python
  - ${program}
  - ${args}
